import { useSelector } from "react-redux"
import Newfun from "./constant"

const Search = (ser="new english trailer")=>{
    console.log('from the search');
    Newfun(ser)
    
}
export default Search